package reflectioninvoker;

import reflectioninvoker.targets.Target;

/**
 * Invokes methods of Target class using provided target class instance.
 * Under construction, WIP.
 */
public class InstanceMethodReflectionInvoker implements ReflectionInvoker{
    @Override
    public Object invoke(Target target) {
        return null;
    }
}
